<h1> Your filtered links are: </h1>
<ul id='links'>
  <% @links.each do |link| %>
  <% if link.tags.map(&:tags) == ["#{@search}"]  %>
    <li>
      URL:    <%= link.url %>
      TITLE:  <%= link.title %>
      TAG     <%= link.tags.map(&:tags) %>
    </li>
  <% end %>
  <% end %>
</ul>
